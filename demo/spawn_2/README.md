# GameTODO Bot

Проект: Telegram-бот для управления задачами с игровой механикой.

## Быстрый старт

1) Скопируйте `.env.example` в `.env` и заполните `BOT_TOKEN`.
2) Запустите:

```bash
docker-compose up --build
```

## Документация

Полная спецификация и требования — в `docs/`.
